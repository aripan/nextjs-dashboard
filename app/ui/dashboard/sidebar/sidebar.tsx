import React, { useState } from "react";
// Define the Props interface
export interface ISidebarProps {}

const SidebarUI: React.FunctionComponent<ISidebarProps> = (props) => {
  return <div>SidebarUI</div>;
};
export default SidebarUI;
